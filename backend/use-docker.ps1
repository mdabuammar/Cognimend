# Add Docker to PATH for this session
$env:PATH += ";C:\Program Files\Docker\Docker\resources\bin"
Write-Host "✅ Docker added to PATH. You can now use 'docker' commands directly." -ForegroundColor Green
Write-Host "Example: docker ps" -ForegroundColor Yellow
